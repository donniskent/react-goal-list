import GoalsList from "./GoalsList"
const Goals = (props) =>
{
    return (
        
        <div>

            <GoalsList items={props.items}/>


        </div>
        //return the expenses list
    
    
    
        );
}
export default Goals;